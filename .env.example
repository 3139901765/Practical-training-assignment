# AI 问答的大模型配置（配置驱动，自动降级）
# LLM_MODE: auto（有 key 用 DeepSeek，无 key 用内置 mock）| deepseek（强制真实调用）| mock（强制 mock）
LLM_MODE=auto
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# 服务端口
PORT=3001
